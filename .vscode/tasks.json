{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "compile and run C",
			"type": "shell",
			"command": "g++ -std=c++11 ${file} -o ${fileBasenameNoExtension}; ./${fileBasenameNoExtension} ",
			"presentation": {
				"reveal": "always",
				"panel": "shared"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
  }